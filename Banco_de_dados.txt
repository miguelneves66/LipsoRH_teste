DROP DATABASE IF EXISTS lipson_db;
CREATE DATABASE IF NOT EXISTS lipson_db;
USE lipson_db;

CREATE TABLE usuario (
    cpf VARCHAR(11) PRIMARY KEY NOT NULL,
    nome VARCHAR(100) NOT NULL,
    endereco VARCHAR(100) NOT NULL,
    data_admissao DATE,
    cargo VARCHAR(50),
    senha VARCHAR(255)
);

CREATE TABLE IF NOT EXISTS rh (
    cpf VARCHAR(11) PRIMARY KEY NOT NULL,
    nome VARCHAR(100) NOT NULL,
    endereco VARCHAR(100) NOT NULL,
    data_admissao DATE NOT NULL,
    cargo VARCHAR(50), 
    senha VARCHAR(255) NOT NULL
);

INSERT INTO usuario (cpf, nome, endereco, data_admissao, cargo, senha) 
VALUES ('12345678901','Pedro Guerdis','Rua Exemplo, 123','2024-01-10','Analista','hash_da_senha_aqui');

INSERT INTO rh (cpf,nome,endereco,data_admissao,cargo,senha) 
VALUES ('98765432100','Gestor Administrativo','Av. Industrial, 500','2026-05-13','Gerente','admin_hash');

SELECT * FROM usuario;




/*Comandos para ligar o servidor

./mvnw spring-boot:run

ls

$env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"

echo $env:JAVA_HOME*/
